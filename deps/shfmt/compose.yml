name: shfmt

include:
  - ../go/compose.yml

services:
  shfmt:
    image: shfmt
    command: /usr/bin/tail -f /dev/null
    environment:
      PATH: ${ENV_SHFMT_PATH}
    env_file:
      - ../default.env
      - override.env
    build:
      dockerfile: Dockerfile
      tags:
        - shfmt
      args:
        GOROOT: ${ENV_GOROOT}
        GOBIN: ${ENV_GOBIN}
        USERNAME: ${ENV_USERNAME}
    depends_on:
      go:
        required: true
        condition: service_started
